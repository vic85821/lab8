prob1
    because throw have the function of return()
    so after throw
    main function will return

prob2
    the same as prob1
    catch block will return and over
    and continue execute cout<<"I love C++"<<endl;
    show it will show

prob3
    compile it with makefile
    if new Mino fail
    it will go to function my_handler
    output error message and exit program

